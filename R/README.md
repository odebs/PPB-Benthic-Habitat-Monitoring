# R analysis

Run `PPB_validation_temporal_QA_analysis.R` from the repository root.

Suggested packages: `dplyr`, `caret`, `ggplot2`, `tidyr`, `tidyverse`, `terra`, `patchwork`. Base package `grid` is also used.

The script uses the archived validation, annual-area, turbidity, sensitivity and persistence products in `data/`.
